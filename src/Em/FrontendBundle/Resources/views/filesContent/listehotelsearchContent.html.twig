<div class="col-md-9 site-main-content">
    <main id="main" class="site-main">
        <div class="page-listing-control clearfix">
            <div class="row">
                <div class="col-xs-6 col-lg-7">
                    <h3 class="heading results">{{ dataentity|length }} Hôtel(s) trouvé(s)</h3>
                </div>
                <div class="col-xs-6 col-lg-5 page-controls-wrapper">
                    <div class="sort-controls">
                        <select name="sort-properties" id="sort-properties" onchange="this.options[this.selectedIndex].value && (window.location = this.options[this.selectedIndex].value);">
                            <option value="">Rechercher par pays</option>

                        </select>
                    </div>
                    <!-- .sort-controls -->
                    <p> </p>
                </div>

            </div>
            <div class="row">

                <!-- .page-controls-wrapper -->
                <div class="entry-content clearfix">
                    <p> </p>
                    {% if service.contenus is defined %}
                      {{ service.contenus|raw }}
                    {% endif %}
                </div>
            </div>
            <!-- .row -->
        </div>
        <!-- .page-listing-control -->
        <!-- .property-listing-simple-2 -->
        {% for data in pagination %}
        <article class="property-listing-simple property-listing-simple-2 hentry clearfix">
            <div class="property-thumbnail col-sm-5 zero-horizontal-padding">
                <div class="price-wrapper">
                    <span class="price">Voir les tarifs </span>
                </div>
                <a href="{{ path('em_frontend_hotelecaracteristique',{'idagence': data.id}) }}">
                {{ render(controller('EmFrontendBundle:Frontend:listeImageAgence', {'entity': data })) }}
                </a>
            </div>
            <!-- .property-thumbnail -->
            <div class="title-and-meta col-sm-7">
                <header class="entry-header">
                    <h3 class="entry-title"><span class="icon-group booking-item-rating-stars">
                            {%  set num = 1 %}
                            {% for  num in  1..data.grade %}
                                <i class="fa fa-star"></i>
                                {%  set num = num +1 %}
                            {% endfor %}
                        </span><a href="{{ path('em_frontend_hotelecaracteristique',{'idagence': data.id}) }}" rel="bookmark">{{ data.nom }} | {{ data.ville}}</a></h3>
                </header>
                <div class="property-content">
                    <p>{{ data.description }}</p>


                </div>
                <div class="property-meta entry-meta clearfix">

                   <!-- <div class="meta-item">
                        <i class="fa fa-user" > </i>
                    </div> -->

                </div>
                <!-- .property-meta -->
            </div>
            <!-- .title-and-meta -->
        </article>
        {% endfor %}
        <!-- .property-listing-simple-2 -->
        <div class="pagination">
            {{ knp_pagination_render(pagination) }}
        </div>
        <!-- .pagination -->
    </main>
    <!-- .site-main -->
</div>
